extends ../main-frame.pug

block content
    div.freeSpace

    .container-fluid
        .row
            .col-md-10.col-md-offset-1
                .col-md-3.text-center
                    a.thumbnail
                        img(src= user.profilePicture.src, alt="profile-pic")
                    h2 #{user.username}
                    a.btn.profile(href="/update-info") Update Info
                block rightSide

    div.freeSpace